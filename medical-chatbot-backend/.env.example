# ============================================================
# ENVIRONMENT VARIABLES — copy this file to .env and fill in
# ============================================================

# --- Database ---
# Local SQLite (development):
DATABASE_URL=sqlite:///./sql_app.db
# Production PostgreSQL on Render / Neon (uncomment and fill):
# DATABASE_URL=postgresql://username:password@host:5432/medical_chatbot

# --- JWT Configuration ---
# CRITICAL: Use the SAME value on every environment (local, Render, Vercel)
# to avoid "Invalid Token" errors.
# Generate a strong key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-replace-with-random-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080   # 7 days for registered users

# --- OpenAI ---
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# --- WebTeb Symptom Checker API (optional) ---
WEBTEB_API_KEY=your-webteb-api-key-here
WEBTEB_API_URL=https://api.webteb.com/symptom-checker

# --- Application Settings ---
ENVIRONMENT=development          # Set to "production" on Render
DEBUG=True                       # Set to False on Render

# CORS: comma-separated list of allowed origins
# On Render set this to your Vercel URL, e.g.:
# CORS_ORIGINS=https://your-app.vercel.app,https://medical-chatbot-1-m6re.onrender.com
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8000

# ============================================================
# RENDER BUILD COMMAND (paste into Render dashboard):
#   pip install -r requirements.txt && alembic -c alembic.ini upgrade head
#
# RENDER START COMMAND:
#   uvicorn app.main:app --host 0.0.0.0 --port $PORT
#
# VERCEL ENVIRONMENT VARIABLES to set:
#   NEXT_PUBLIC_API_URL = https://medical-chatbot-1-m6re.onrender.com
# ============================================================
