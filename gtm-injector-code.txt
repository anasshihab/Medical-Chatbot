<!--
========================================================================================
  MASSIVE INJECTION SCRIPT - WEBTEB MEDICAL CHATBOT WIDGET
  INSTRUCTIONS:
  1. Add this snippet as a "Custom HTML" tag inside Google Tag Manager.
  2. Set the trigger to "All Pages" (or select specific pages).
  3. Replace 'https://YOUR_VERCEL_APP_URL.vercel.app' with your actual domain below.
========================================================================================
-->
<script>
(function() {
    // Prevent multiple injections
    if (document.getElementById('webteb-medical-chatbot-fab')) return;

    // 1. Create Floating Action Button (FAB)
    const fab = document.createElement('button');
    fab.id = 'webteb-medical-chatbot-fab';
    fab.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="white" width="30px" height="30px"><path d="M12 2C6.48 2 2 5.92 2 10.75c0 2.76 1.48 5.22 3.79 6.79l-1.63 3.65a1 1 0 0 0 1.34 1.25l4.16-2.02c.75.09 1.53.13 2.34.13 5.52 0 10-3.92 10-8.75S17.52 2 12 2zm0 15c-.75 0-1.5-.05-2.22-.16a1 1 0 0 0-.77.16l-2.4 1.17.94-2.11a1 1 0 0 0-.17-1 6.54 6.54 0 0 1-1.59-4.31c0-3.6 3.68-6.5 8.21-6.5s8.21 2.9 8.21 6.5-3.68 6.5-8.21 6.5z"/><path d="M11 7h2v3h3v2h-3v3h-2v-3H8v-2h3z"/></svg>';
    fab.style.cssText = `
        position: fixed;
        bottom: 24px;
        right: 24px;
        width: 65px;
        height: 65px;
        border-radius: 50%;
        background: linear-gradient(135deg, #00B3D7 0%, #1D5D74 100%);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        box-shadow: 0 8px 32px rgba(0, 179, 215, 0.4);
        cursor: pointer;
        z-index: 2147483647; /* Highest possible z-index */
        display: flex;
        align-items: center;
        justify-content: center;
        transition: transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275), box-shadow 0.3s ease;
        padding: 0;
        margin: 0;
        outline: none;
    `;
    
    // Hover effects
    fab.addEventListener('mouseover', function() {
        fab.style.transform = 'scale(1.1)';
        fab.style.boxShadow = '0 12px 40px rgba(0, 179, 215, 0.6)';
    });
    fab.addEventListener('mouseout', function() {
        fab.style.transform = 'scale(1)';
        fab.style.boxShadow = '0 8px 32px rgba(0, 179, 215, 0.4)';
    });

    // 2. Create Hidden Iframe
    const iframe = document.createElement('iframe');
    // NOTE: Replace this domain with your final standalone widget URL.
iframe.src = 'https://medical-chatbot-git-feature-usage-limits-anas-projects-30c1c013.vercel.app/widget.html';
    iframe.style.cssText = `
        position: fixed;
        bottom: 100px;
        right: 24px;
        width: 380px;
        height: 600px;
        max-width: calc(100vw - 48px);
        max-height: calc(100vh - 120px);
        border: none;
        border-radius: 20px;
        background: transparent;
        box-shadow: 0 20px 50px rgba(0, 0, 0, 0.15), inset 0 0 0 1px rgba(255, 255, 255, 0.6);
        z-index: 2147483646;
        opacity: 0;
        visibility: hidden;
        transform: translateY(20px);
        transition: opacity 0.4s ease, transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275), visibility 0.4s;
    `;

    // 3. Toggle Logic
    let isOpen = false;
    fab.addEventListener('click', function() {
        isOpen = !isOpen;
        if (isOpen) {
            iframe.style.visibility = 'visible';
            iframe.style.opacity = '1';
            iframe.style.transform = 'translateY(0)';
            // Change to 'Close' icon (X)
            fab.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="white" width="30px" height="30px"><path d="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"/></svg>';
            fab.style.background = 'linear-gradient(135deg, #1D5D74 0%, #113c4a 100%)'; // Darker theme for close
        } else {
            iframe.style.opacity = '0';
            iframe.style.transform = 'translateY(20px)';
            // Revert to 'Chat' icon
            fab.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="white" width="30px" height="30px"><path d="M12 2C6.48 2 2 5.92 2 10.75c0 2.76 1.48 5.22 3.79 6.79l-1.63 3.65a1 1 0 0 0 1.34 1.25l4.16-2.02c.75.09 1.53.13 2.34.13 5.52 0 10-3.92 10-8.75S17.52 2 12 2zm0 15c-.75 0-1.5-.05-2.22-.16a1 1 0 0 0-.77.16l-2.4 1.17.94-2.11a1 1 0 0 0-.17-1 6.54 6.54 0 0 1-1.59-4.31c0-3.6 3.68-6.5 8.21-6.5s8.21 2.9 8.21 6.5-3.68 6.5-8.21 6.5z"/><path d="M11 7h2v3h3v2h-3v3h-2v-3H8v-2h3z"/></svg>';
            fab.style.background = 'linear-gradient(135deg, #00B3D7 0%, #1D5D74 100%)';
            setTimeout(function() {
                if (!isOpen) iframe.style.visibility = 'hidden';
            }, 400);
        }
    });

    // 4. Onload event to read window.dataLayer and postMessage to Iframe
    iframe.onload = function() {
        let tier = 'Guest';
        let token = null;

        // Traverse DataLayer to find latest User attributes
        window.dataLayer = window.dataLayer || [];
        for (let i = window.dataLayer.length - 1; i >= 0; i--) {
            const data = window.dataLayer[i];
            if (data && data.userTier) tier = data.userTier;
            if (data && data.userToken) token = data.userToken;
        }

        // Post secure payload back to the iframe explicitly
        iframe.contentWindow.postMessage({
            type: 'AUTH_DATA',
            tier: tier,
            token: token
        }, '*'); // '*' allows connection since domains typically differ. Change later to target exact widget origin if needed.
    };

    // 5. Append Elements
    document.body.appendChild(iframe);
    document.body.appendChild(fab);
})();
</script>
